// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/openmesh/booking/ent/booking"
	"github.com/openmesh/booking/ent/bookingmetadatum"
	"github.com/openmesh/booking/ent/predicate"
)

// BookingMetadatumUpdate is the builder for updating BookingMetadatum entities.
type BookingMetadatumUpdate struct {
	config
	hooks    []Hook
	mutation *BookingMetadatumMutation
}

// Where appends a list predicates to the BookingMetadatumUpdate builder.
func (bmu *BookingMetadatumUpdate) Where(ps ...predicate.BookingMetadatum) *BookingMetadatumUpdate {
	bmu.mutation.Where(ps...)
	return bmu
}

// SetKey sets the "key" field.
func (bmu *BookingMetadatumUpdate) SetKey(s string) *BookingMetadatumUpdate {
	bmu.mutation.SetKey(s)
	return bmu
}

// SetValue sets the "value" field.
func (bmu *BookingMetadatumUpdate) SetValue(s string) *BookingMetadatumUpdate {
	bmu.mutation.SetValue(s)
	return bmu
}

// SetBookingId sets the "bookingId" field.
func (bmu *BookingMetadatumUpdate) SetBookingId(i int) *BookingMetadatumUpdate {
	bmu.mutation.SetBookingId(i)
	return bmu
}

// SetBookingID sets the "booking" edge to the Booking entity by ID.
func (bmu *BookingMetadatumUpdate) SetBookingID(id int) *BookingMetadatumUpdate {
	bmu.mutation.SetBookingID(id)
	return bmu
}

// SetBooking sets the "booking" edge to the Booking entity.
func (bmu *BookingMetadatumUpdate) SetBooking(b *Booking) *BookingMetadatumUpdate {
	return bmu.SetBookingID(b.ID)
}

// Mutation returns the BookingMetadatumMutation object of the builder.
func (bmu *BookingMetadatumUpdate) Mutation() *BookingMetadatumMutation {
	return bmu.mutation
}

// ClearBooking clears the "booking" edge to the Booking entity.
func (bmu *BookingMetadatumUpdate) ClearBooking() *BookingMetadatumUpdate {
	bmu.mutation.ClearBooking()
	return bmu
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (bmu *BookingMetadatumUpdate) Save(ctx context.Context) (int, error) {
	var (
		err      error
		affected int
	)
	if len(bmu.hooks) == 0 {
		if err = bmu.check(); err != nil {
			return 0, err
		}
		affected, err = bmu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*BookingMetadatumMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = bmu.check(); err != nil {
				return 0, err
			}
			bmu.mutation = mutation
			affected, err = bmu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(bmu.hooks) - 1; i >= 0; i-- {
			if bmu.hooks[i] == nil {
				return 0, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = bmu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, bmu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (bmu *BookingMetadatumUpdate) SaveX(ctx context.Context) int {
	affected, err := bmu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (bmu *BookingMetadatumUpdate) Exec(ctx context.Context) error {
	_, err := bmu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (bmu *BookingMetadatumUpdate) ExecX(ctx context.Context) {
	if err := bmu.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (bmu *BookingMetadatumUpdate) check() error {
	if _, ok := bmu.mutation.BookingID(); bmu.mutation.BookingCleared() && !ok {
		return errors.New("ent: clearing a required unique edge \"booking\"")
	}
	return nil
}

func (bmu *BookingMetadatumUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   bookingmetadatum.Table,
			Columns: bookingmetadatum.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: bookingmetadatum.FieldID,
			},
		},
	}
	if ps := bmu.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := bmu.mutation.Key(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bookingmetadatum.FieldKey,
		})
	}
	if value, ok := bmu.mutation.Value(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bookingmetadatum.FieldValue,
		})
	}
	if bmu.mutation.BookingCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   bookingmetadatum.BookingTable,
			Columns: []string{bookingmetadatum.BookingColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: booking.FieldID,
				},
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := bmu.mutation.BookingIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   bookingmetadatum.BookingTable,
			Columns: []string{bookingmetadatum.BookingColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: booking.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, bmu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{bookingmetadatum.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return 0, err
	}
	return n, nil
}

// BookingMetadatumUpdateOne is the builder for updating a single BookingMetadatum entity.
type BookingMetadatumUpdateOne struct {
	config
	fields   []string
	hooks    []Hook
	mutation *BookingMetadatumMutation
}

// SetKey sets the "key" field.
func (bmuo *BookingMetadatumUpdateOne) SetKey(s string) *BookingMetadatumUpdateOne {
	bmuo.mutation.SetKey(s)
	return bmuo
}

// SetValue sets the "value" field.
func (bmuo *BookingMetadatumUpdateOne) SetValue(s string) *BookingMetadatumUpdateOne {
	bmuo.mutation.SetValue(s)
	return bmuo
}

// SetBookingId sets the "bookingId" field.
func (bmuo *BookingMetadatumUpdateOne) SetBookingId(i int) *BookingMetadatumUpdateOne {
	bmuo.mutation.SetBookingId(i)
	return bmuo
}

// SetBookingID sets the "booking" edge to the Booking entity by ID.
func (bmuo *BookingMetadatumUpdateOne) SetBookingID(id int) *BookingMetadatumUpdateOne {
	bmuo.mutation.SetBookingID(id)
	return bmuo
}

// SetBooking sets the "booking" edge to the Booking entity.
func (bmuo *BookingMetadatumUpdateOne) SetBooking(b *Booking) *BookingMetadatumUpdateOne {
	return bmuo.SetBookingID(b.ID)
}

// Mutation returns the BookingMetadatumMutation object of the builder.
func (bmuo *BookingMetadatumUpdateOne) Mutation() *BookingMetadatumMutation {
	return bmuo.mutation
}

// ClearBooking clears the "booking" edge to the Booking entity.
func (bmuo *BookingMetadatumUpdateOne) ClearBooking() *BookingMetadatumUpdateOne {
	bmuo.mutation.ClearBooking()
	return bmuo
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (bmuo *BookingMetadatumUpdateOne) Select(field string, fields ...string) *BookingMetadatumUpdateOne {
	bmuo.fields = append([]string{field}, fields...)
	return bmuo
}

// Save executes the query and returns the updated BookingMetadatum entity.
func (bmuo *BookingMetadatumUpdateOne) Save(ctx context.Context) (*BookingMetadatum, error) {
	var (
		err  error
		node *BookingMetadatum
	)
	if len(bmuo.hooks) == 0 {
		if err = bmuo.check(); err != nil {
			return nil, err
		}
		node, err = bmuo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*BookingMetadatumMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = bmuo.check(); err != nil {
				return nil, err
			}
			bmuo.mutation = mutation
			node, err = bmuo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(bmuo.hooks) - 1; i >= 0; i-- {
			if bmuo.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = bmuo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, bmuo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (bmuo *BookingMetadatumUpdateOne) SaveX(ctx context.Context) *BookingMetadatum {
	node, err := bmuo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (bmuo *BookingMetadatumUpdateOne) Exec(ctx context.Context) error {
	_, err := bmuo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (bmuo *BookingMetadatumUpdateOne) ExecX(ctx context.Context) {
	if err := bmuo.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (bmuo *BookingMetadatumUpdateOne) check() error {
	if _, ok := bmuo.mutation.BookingID(); bmuo.mutation.BookingCleared() && !ok {
		return errors.New("ent: clearing a required unique edge \"booking\"")
	}
	return nil
}

func (bmuo *BookingMetadatumUpdateOne) sqlSave(ctx context.Context) (_node *BookingMetadatum, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   bookingmetadatum.Table,
			Columns: bookingmetadatum.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: bookingmetadatum.FieldID,
			},
		},
	}
	id, ok := bmuo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing BookingMetadatum.ID for update")}
	}
	_spec.Node.ID.Value = id
	if fields := bmuo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bookingmetadatum.FieldID)
		for _, f := range fields {
			if !bookingmetadatum.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
			}
			if f != bookingmetadatum.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := bmuo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := bmuo.mutation.Key(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bookingmetadatum.FieldKey,
		})
	}
	if value, ok := bmuo.mutation.Value(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bookingmetadatum.FieldValue,
		})
	}
	if bmuo.mutation.BookingCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   bookingmetadatum.BookingTable,
			Columns: []string{bookingmetadatum.BookingColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: booking.FieldID,
				},
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := bmuo.mutation.BookingIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   bookingmetadatum.BookingTable,
			Columns: []string{bookingmetadatum.BookingColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: booking.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	_node = &BookingMetadatum{config: bmuo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, bmuo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{bookingmetadatum.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	return _node, nil
}
