// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/openmesh/booking/ent/booking"
	"github.com/openmesh/booking/ent/bookingmetadatum"
)

// BookingMetadatumCreate is the builder for creating a BookingMetadatum entity.
type BookingMetadatumCreate struct {
	config
	mutation *BookingMetadatumMutation
	hooks    []Hook
}

// SetKey sets the "key" field.
func (bmc *BookingMetadatumCreate) SetKey(s string) *BookingMetadatumCreate {
	bmc.mutation.SetKey(s)
	return bmc
}

// SetValue sets the "value" field.
func (bmc *BookingMetadatumCreate) SetValue(s string) *BookingMetadatumCreate {
	bmc.mutation.SetValue(s)
	return bmc
}

// SetBookingId sets the "bookingId" field.
func (bmc *BookingMetadatumCreate) SetBookingId(i int) *BookingMetadatumCreate {
	bmc.mutation.SetBookingId(i)
	return bmc
}

// SetBookingID sets the "booking" edge to the Booking entity by ID.
func (bmc *BookingMetadatumCreate) SetBookingID(id int) *BookingMetadatumCreate {
	bmc.mutation.SetBookingID(id)
	return bmc
}

// SetBooking sets the "booking" edge to the Booking entity.
func (bmc *BookingMetadatumCreate) SetBooking(b *Booking) *BookingMetadatumCreate {
	return bmc.SetBookingID(b.ID)
}

// Mutation returns the BookingMetadatumMutation object of the builder.
func (bmc *BookingMetadatumCreate) Mutation() *BookingMetadatumMutation {
	return bmc.mutation
}

// Save creates the BookingMetadatum in the database.
func (bmc *BookingMetadatumCreate) Save(ctx context.Context) (*BookingMetadatum, error) {
	var (
		err  error
		node *BookingMetadatum
	)
	if len(bmc.hooks) == 0 {
		if err = bmc.check(); err != nil {
			return nil, err
		}
		node, err = bmc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*BookingMetadatumMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = bmc.check(); err != nil {
				return nil, err
			}
			bmc.mutation = mutation
			if node, err = bmc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(bmc.hooks) - 1; i >= 0; i-- {
			if bmc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = bmc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, bmc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (bmc *BookingMetadatumCreate) SaveX(ctx context.Context) *BookingMetadatum {
	v, err := bmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (bmc *BookingMetadatumCreate) Exec(ctx context.Context) error {
	_, err := bmc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (bmc *BookingMetadatumCreate) ExecX(ctx context.Context) {
	if err := bmc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (bmc *BookingMetadatumCreate) check() error {
	if _, ok := bmc.mutation.Key(); !ok {
		return &ValidationError{Name: "key", err: errors.New(`ent: missing required field "key"`)}
	}
	if _, ok := bmc.mutation.Value(); !ok {
		return &ValidationError{Name: "value", err: errors.New(`ent: missing required field "value"`)}
	}
	if _, ok := bmc.mutation.BookingId(); !ok {
		return &ValidationError{Name: "bookingId", err: errors.New(`ent: missing required field "bookingId"`)}
	}
	if _, ok := bmc.mutation.BookingID(); !ok {
		return &ValidationError{Name: "booking", err: errors.New("ent: missing required edge \"booking\"")}
	}
	return nil
}

func (bmc *BookingMetadatumCreate) sqlSave(ctx context.Context) (*BookingMetadatum, error) {
	_node, _spec := bmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, bmc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (bmc *BookingMetadatumCreate) createSpec() (*BookingMetadatum, *sqlgraph.CreateSpec) {
	var (
		_node = &BookingMetadatum{config: bmc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: bookingmetadatum.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: bookingmetadatum.FieldID,
			},
		}
	)
	if value, ok := bmc.mutation.Key(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bookingmetadatum.FieldKey,
		})
		_node.Key = value
	}
	if value, ok := bmc.mutation.Value(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bookingmetadatum.FieldValue,
		})
		_node.Value = value
	}
	if nodes := bmc.mutation.BookingIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   bookingmetadatum.BookingTable,
			Columns: []string{bookingmetadatum.BookingColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: booking.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.BookingId = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// BookingMetadatumCreateBulk is the builder for creating many BookingMetadatum entities in bulk.
type BookingMetadatumCreateBulk struct {
	config
	builders []*BookingMetadatumCreate
}

// Save creates the BookingMetadatum entities in the database.
func (bmcb *BookingMetadatumCreateBulk) Save(ctx context.Context) ([]*BookingMetadatum, error) {
	specs := make([]*sqlgraph.CreateSpec, len(bmcb.builders))
	nodes := make([]*BookingMetadatum, len(bmcb.builders))
	mutators := make([]Mutator, len(bmcb.builders))
	for i := range bmcb.builders {
		func(i int, root context.Context) {
			builder := bmcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*BookingMetadatumMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, bmcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, bmcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, bmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (bmcb *BookingMetadatumCreateBulk) SaveX(ctx context.Context) []*BookingMetadatum {
	v, err := bmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (bmcb *BookingMetadatumCreateBulk) Exec(ctx context.Context) error {
	_, err := bmcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (bmcb *BookingMetadatumCreateBulk) ExecX(ctx context.Context) {
	if err := bmcb.Exec(ctx); err != nil {
		panic(err)
	}
}
