// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/openmesh/booking/ent/resource"
	"github.com/openmesh/booking/ent/slot"
)

// SlotCreate is the builder for creating a Slot entity.
type SlotCreate struct {
	config
	mutation *SlotMutation
	hooks    []Hook
}

// SetDay sets the "day" field.
func (sc *SlotCreate) SetDay(s string) *SlotCreate {
	sc.mutation.SetDay(s)
	return sc
}

// SetStartTime sets the "startTime" field.
func (sc *SlotCreate) SetStartTime(s string) *SlotCreate {
	sc.mutation.SetStartTime(s)
	return sc
}

// SetEndTime sets the "endTime" field.
func (sc *SlotCreate) SetEndTime(s string) *SlotCreate {
	sc.mutation.SetEndTime(s)
	return sc
}

// SetQuantity sets the "quantity" field.
func (sc *SlotCreate) SetQuantity(i int) *SlotCreate {
	sc.mutation.SetQuantity(i)
	return sc
}

// SetNillableQuantity sets the "quantity" field if the given value is not nil.
func (sc *SlotCreate) SetNillableQuantity(i *int) *SlotCreate {
	if i != nil {
		sc.SetQuantity(*i)
	}
	return sc
}

// SetResourceId sets the "resourceId" field.
func (sc *SlotCreate) SetResourceId(i int) *SlotCreate {
	sc.mutation.SetResourceId(i)
	return sc
}

// SetResourceID sets the "resource" edge to the Resource entity by ID.
func (sc *SlotCreate) SetResourceID(id int) *SlotCreate {
	sc.mutation.SetResourceID(id)
	return sc
}

// SetResource sets the "resource" edge to the Resource entity.
func (sc *SlotCreate) SetResource(r *Resource) *SlotCreate {
	return sc.SetResourceID(r.ID)
}

// Mutation returns the SlotMutation object of the builder.
func (sc *SlotCreate) Mutation() *SlotMutation {
	return sc.mutation
}

// Save creates the Slot in the database.
func (sc *SlotCreate) Save(ctx context.Context) (*Slot, error) {
	var (
		err  error
		node *Slot
	)
	if len(sc.hooks) == 0 {
		if err = sc.check(); err != nil {
			return nil, err
		}
		node, err = sc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SlotMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sc.check(); err != nil {
				return nil, err
			}
			sc.mutation = mutation
			if node, err = sc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(sc.hooks) - 1; i >= 0; i-- {
			if sc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = sc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sc *SlotCreate) SaveX(ctx context.Context) *Slot {
	v, err := sc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sc *SlotCreate) Exec(ctx context.Context) error {
	_, err := sc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sc *SlotCreate) ExecX(ctx context.Context) {
	if err := sc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sc *SlotCreate) check() error {
	if _, ok := sc.mutation.Day(); !ok {
		return &ValidationError{Name: "day", err: errors.New(`ent: missing required field "day"`)}
	}
	if _, ok := sc.mutation.StartTime(); !ok {
		return &ValidationError{Name: "startTime", err: errors.New(`ent: missing required field "startTime"`)}
	}
	if _, ok := sc.mutation.EndTime(); !ok {
		return &ValidationError{Name: "endTime", err: errors.New(`ent: missing required field "endTime"`)}
	}
	if _, ok := sc.mutation.ResourceId(); !ok {
		return &ValidationError{Name: "resourceId", err: errors.New(`ent: missing required field "resourceId"`)}
	}
	if _, ok := sc.mutation.ResourceID(); !ok {
		return &ValidationError{Name: "resource", err: errors.New("ent: missing required edge \"resource\"")}
	}
	return nil
}

func (sc *SlotCreate) sqlSave(ctx context.Context) (*Slot, error) {
	_node, _spec := sc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sc *SlotCreate) createSpec() (*Slot, *sqlgraph.CreateSpec) {
	var (
		_node = &Slot{config: sc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: slot.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: slot.FieldID,
			},
		}
	)
	if value, ok := sc.mutation.Day(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: slot.FieldDay,
		})
		_node.Day = value
	}
	if value, ok := sc.mutation.StartTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: slot.FieldStartTime,
		})
		_node.StartTime = value
	}
	if value, ok := sc.mutation.EndTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: slot.FieldEndTime,
		})
		_node.EndTime = value
	}
	if value, ok := sc.mutation.Quantity(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: slot.FieldQuantity,
		})
		_node.Quantity = &value
	}
	if nodes := sc.mutation.ResourceIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   slot.ResourceTable,
			Columns: []string{slot.ResourceColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: resource.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ResourceId = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SlotCreateBulk is the builder for creating many Slot entities in bulk.
type SlotCreateBulk struct {
	config
	builders []*SlotCreate
}

// Save creates the Slot entities in the database.
func (scb *SlotCreateBulk) Save(ctx context.Context) ([]*Slot, error) {
	specs := make([]*sqlgraph.CreateSpec, len(scb.builders))
	nodes := make([]*Slot, len(scb.builders))
	mutators := make([]Mutator, len(scb.builders))
	for i := range scb.builders {
		func(i int, root context.Context) {
			builder := scb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SlotMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, scb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, scb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, scb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (scb *SlotCreateBulk) SaveX(ctx context.Context) []*Slot {
	v, err := scb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (scb *SlotCreateBulk) Exec(ctx context.Context) error {
	_, err := scb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (scb *SlotCreateBulk) ExecX(ctx context.Context) {
	if err := scb.Exec(ctx); err != nil {
		panic(err)
	}
}
